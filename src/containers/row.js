import React from 'react'
//import MovieCard from '../components/MovieCard'
const Row = (props) => {
    //<MovieCard key={props} movie={props}/> 
    // move to Movie.js 
    return (  
    <div>
   
    </div>
    );
}
 
export default Row;

